[package]
name = "street-concept-designer-project-io"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = { version = "=1.0.151", features = ["float_roundtrip"] }
street-concept-designer-kernel = { path = "../.." }
street-concept-designer-project-core = { path = "../project-core" }

[[bench]]
name = "r2b_persistence"
harness = false
